#pragma once
#include <vector>

// Declare your lis_len interface here.


std::vector<int> longestIncreasingSubsequence(const std::vector<int> &arr );
int longestIncreasingSubsequenceLen(const std::vector<int> &arr );
int lisWithBinarySearchLen(const std::vector<int> &arr );
