#pragma once

#include <string>
#include <vector>

std::vector<int> smallerNumbersThanCurrentBF(const std::vector<int>& nums);

std::vector<int> smallerNumbersThanCurrent(const std::vector<int>& nums);